services:
  - type: web
    name: terrasynapse-backend
    env: python
    buildCommand: pip install --no-cache-dir --only-binary=all -r backend/requirements.txt
    startCommand: cd backend && python main.py
